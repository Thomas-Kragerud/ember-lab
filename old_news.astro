  <section class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-8 my-12">
    <!-- Same content as before -->
    <h2 class="text-3xl font-semibold mb-8 text-gray-800 dark:text-white">Latest News</h2>
    {latestNews.map((post) => (
      <article class="bg-gray-50 dark:bg-gray-700 p-6 rounded-lg shadow-sm mb-6 transform translate-y-0 hover-lift">
        <h3 class="text-2xl font-bold mb-2 text-gray-800 dark:text-white">{post.title}</h3>
        <p class="text-sm text-gray-600 dark:text-gray-300 mb-3">
          {post.date.toLocaleDateString('en-US', {year: 'numeric', month: 'long', day: 'numeric'})}
        </p>
        <p class="text-gray-700 dark:text-gray-200">{post.summary}</p>
        <a href={`/news/${post.slug || '#'}`} class="inline-flex items-center mt-4 text-emberBlue-600 hover:text-emberBlue-700 dark:text-emberBlue-300 dark:hover:text-emberBlue-200">
          Read more
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
        </a>
      </article>
    ))}
    <div class="text-center mt-8">
      <a href="/news" class="px-5 py-2 bg-emberBlue-500 hover:bg-emberBlue-600 text-white rounded-lg transition-all duration-300 inline-block hover:-translate-y-1">
        View All News
      </a>
    </div>
  </section>




<article class="news-card bg-gray-50 dark:bg-gray-700 p-6 rounded-lg shadow-sm mb-6 transform translate-y-0 hover-lift">
  <h3 class="text-2xl font-bold mb-2 text-gray-800 dark:text-white">
    {title}
  </h3>
  <p class="text-sm text-gray-600 dark:text-gray-300 mb-3">
    {new Date(date).toLocaleDateString('en-US', {year: 'numeric', month: 'long', day: 'numeric'})}
  </p>
  {summary && <p class="text-gray-700 dark:text-gray-200">{summary}</p>}
  
  <button class="card-trigger inline-flex items-center mt-4 text-emberBlue-600 hover:text-emberBlue-700 dark:text-emberBlue-300 dark:hover:text-emberBlue-200">
    Read more
    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
    </svg>
  </button>
  
  <!-- Keep the popup part -->
  <div class="news-popup fixed inset-0 z-50 flex items-center justify-center bg-emberGray-900/30 dark:bg-emberGray-900/40 backdrop-blur-sm p-4 opacity-0 pointer-events-none transition-all duration-300">
    <!-- Popup content remains the same -->
    <div class="popup-content relative w-full max-w-3xl max-h-[90vh] overflow-y-auto bg-white/90 dark:bg-gray-800/90 backdrop-blur-md rounded-lg shadow-ember border border-gray-100 dark:border-gray-700 p-6 transition-standard">
      <button class="popup-close absolute top-3 right-3 text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
        <span class="sr-only">Close</span>
      </button>
      <article class="prose dark:prose-invert prose-img:rounded-lg prose-headings:font-bold prose-a:text-emberBlue-500 max-w-none">
        <h2>{title}</h2>
        <p class="text-sm text-gray-500 dark:text-gray-400 -mt-4">
          {new Date(date).toLocaleDateString('en-US', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
          })}
        </p>
        <div set:html={content} />
      </article>
    </div>
  </div>
</article>